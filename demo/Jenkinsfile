pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                git branch: 'main', url: 'https://github.com/venkatesh2608/newrepo.git'
            }
        }
        stage('Build') {
            steps {
                dir('demo') {
                    bat 'mvn clean install -DskipTests'
                }
            }
        }
        stage('Test') {
            steps {
                dir('demo') {
                    bat 'mvn test'
                }
            }
        }   
    }
    post {
        always {
            archiveArtifacts artifacts: '**/target/surefire-reports/Playwright Suite/Headless Browser Automation.html'
        }
    }
}
